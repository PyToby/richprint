[project]
name = "richprint"
version = "0.1.0"
description = "Beautiful terminal output for Python objects"
authors = [{name = "Toby", email = "ucet1.random@gmail.com"}]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"